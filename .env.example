# =============================================================================
# 🛸 AMF-OS SOVEREIGN ELITE - CONFIGURATION MATRIX
# =============================================================================
# Copy this file to .env and adjust to your kinetic requirements.
# Variables marked with [CRITICAL] are essential for kernel boot.

# --- 🧠 CORE AI ENGINE (OLLAMA) ---
# The URL of your local Ollama instance. Default is standard local host.
OLLAMA_HOST=http://localhost:11434

# The primary reasoning model. Llama 4 is recommended for elite performance.
AMFBOT_MODEL=llama4:8b

# --- 🌀 KERNEL & PERFORMANCE ---
# Threading model: Bun.Worker is used for parallel execution (swarm).
# Set to 'serial' to disable worker threads (debugging only).
KERNEL_THREADING=parallel

# Response latency target in milliseconds (informative/log tuning).
TARGET_TTFT_MS=150

# --- 💾 MEMORY & CACHE ---
# URL for the hot-cache state management (Redis/KeyDB).
# Default assumes docker-compose service 'cache-hot'.
REDIS_URL=redis://localhost:6379

# Path for the LanceDB vector database (tactical knowledge base).
LANCEDB_PATH=./data/lancedb

# --- 📪 CONNECTIVITY (TELEGRAM) ---
# [CRITICAL] Your Telegram Bot Token from @BotFather.
TELEGRAM_BOT_TOKEN=xxxxxxxxxxxx:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# [CRITICAL] Your Telegram User ID. Only this ID can control the OS.
# Retrieve it via @userinfobot if unknown.
ADMIN_TELEGRAM_ID=000000000

# --- 🛡️ SECURITY & SANDBOX ---
# Root access session timeout in milliseconds (default: 5 minutes).
ROOT_SESSION_TTL=300000

# Strict mode: Always require manual confirmation for terminal commands.
ALWAYS_CONFIRM_SUDO=false

# Workspace root for sandbox isolation.
WORKSPACE_ROOT=/Users/wiaamhadara/AMFbot-Suite

# --- 🎬 MEDIA GENERATION (OPTIONAL) ---
# Model directory for images/video weights.
AMFBOT_MODELS_DIR=./models

# Output directory for generated media artifacts.
AMFBOT_OUTPUT_DIR=./outputs

# Port for the FastAPI media generation bridge.
AMFBOT_MEDIA_PORT=8765

# --- 🛠️ LOGGING & AUDIT ---
# Log level: debug | info | warn | error
LOG_LEVEL=info

# Enable proactive sentinel audit (writes to ~/.amf-os/audit.json).
SENTINEL_AUDIT_ENABLED=true
