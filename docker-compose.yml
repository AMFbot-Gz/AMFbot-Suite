version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: amf-os-cache
    ports:
      - "6379:6379"
    restart: always

  # Ollama is expected to run natively for GPU access, 
  # but can be added here if CPU-only is preferred.
  # ollama:
  #   image: ollama/ollama
  #   container_name: amf-os-ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama:/root/.ollama

volumes:
  ollama:
